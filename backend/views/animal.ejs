<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Animals</title>
  </head>
  <body>
    <h1>Animals</h1>

    <% for (var i=0, x=animals.length; i<x; i++) { %>
      <img src="<%= animals[i].imageUrl %>" />
      <p>Name: <%= animals[i].name %></p>
      <p>Description: <%= animals[i].description %></p>
      <p>Bread: <%= animals[i].breed %></p>
    <% } %>
  </body>
</html>
