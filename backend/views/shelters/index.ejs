<!DOCTYPE html>
<html>
  <head>
    <meta charset="utf-8">
    <title>Shelters</title>
  </head>
  <body>
    <h1>Shelters</h1>

    <ul>
      <% for (var i=0, x=shelters.length; i<x; i++) { %>
        <li><a href="/shelters/<%= shelters[i]._id %>"><%= shelters[i].name %></a></li>
      <% } %>
    </ul>
  </body>
</html>
